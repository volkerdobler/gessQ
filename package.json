{
  "name": "gessq",
  "displayName": "GESS Q.",
  "description": "GESS Q. Support",
  "version": "0.99.0",
  "publisher": "volkerdobler",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages",
    "Snippets"
  ],
  "keywords": [
    "gessq",
    "GESS Q.",
    "gessgroup",
    "online survey cawi",
    "market research",
    "questionnaire"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/volkerdobler/gessQ.git"
  },
  "bugs": {
    "url": "https://github.com/volkerdobler/gessQ/issues"
  },
  "homepage": "https://github.com/volkerdobler/gessQ#readme",
  "license": "AFL-3.0",
  "main": "./out/extension.js",
  "icon": "images/gessQ_icon.png",
  "contributes": {
    "commands": [
      {
        "command": "gessq.showReleaseNotes",
        "title": "Release Notes anzeigen",
        "category": "GESS Q."
      },
      {
        "command": "gessq.resetReleaseNotesState",
        "title": "Release-Notes-Status zurücksetzen",
        "category": "GESS Q."
      }
    ],
    "languages": [
      {
        "id": "gessq",
        "aliases": [
          "GESS Q.",
          "gessQ",
          "gessq"
        ],
        "extensions": [
          ".q"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "gessq",
        "scopeName": "source.gessQ",
        "path": "./language/gessq.tmLanguage.json"
      }
    ],
    "snippets": [
      {
        "language": "gessq",
        "path": "./language/snippets.json"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "GESS Q.",
      "properties": {
        "gessq.logLevel": {
          "type": "string",
          "enum": [
            "off",
            "error",
            "warn",
            "info",
            "debug"
          ],
          "default": "error",
          "description": "Verbosity of the GESS Q. Output channel ('GESS Q.')."
        },
        "gessq.debugMode": {
          "type": "boolean",
          "default": false,
          "deprecationMessage": "Deprecated: use 'gessq.logLevel' instead. When set, this is treated as logLevel = debug.",
          "description": "Deprecated. Enable debug logging (equivalent to gessq.logLevel = debug)."
        },
        "gessq.diagnostics.enable": {
          "type": "boolean",
          "default": true,
          "description": "Report unbalanced brackets, unmatched #macro/#ifdef, missing #include files, duplicate names and unknown #domacro targets."
        },
        "gessq.hover.enable": {
          "type": "boolean",
          "default": true,
          "description": "Show the GESS Q. hover (glossary description for keywords, and the definition location plus code preview for workspace names). Turn off to suppress the GESS Q. hover entirely."
        },
        "gessq.hover.referenceDetail": {
          "type": "string",
          "enum": ["off", "summary", "definition", "full"],
          "enumDescriptions": [
            "No hover when pointing at a reference to a workspace name (keyword hovers still work).",
            "Name, kind and where it is defined. No code, no description, no handbook link (hover the keyword for those).",
            "Also an excerpt of the definition: the question line plus text / title / labels / flt / assert …, without actionblock / javascript / css attributes.",
            "The complete definition verbatim, including actionblock / javascript / css."
          ],
          "default": "summary",
          "description": "How much a hover shows when pointing at a reference to a question or variable defined in the workspace. Requires 'gessq.hover.enable'."
        },
        "gessq.codeLens.definitions": {
          "type": "string",
          "enum": ["off", "questions", "reusable", "all"],
          "enumDescriptions": [
            "No \"N references\" CodeLens.",
            "Only above question definitions (singleq, multiq, singlegridq, …, group).",
            "Above questions plus the reusable building blocks: opennumformat, block / screen, #macro, quotavar.",
            "Also above plain variables (compute, array / vararray / textarray, textelement, intrandom, databaseConnection). Never above set()/load() assignment targets."
          ],
          "default": "reusable",
          "description": "Which definitions carry a \"N references\" CodeLens."
        },
        "gessq.completion.includeWorkspaceSymbols": {
          "type": "boolean",
          "default": true,
          "description": "Offer names defined elsewhere in the workspace (questions, blocks, macros, …) as completions."
        },
        "gessq.files.exclude": {
          "type": "string",
          "default": "",
          "description": "Additional glob pattern excluded from the workspace symbol scan, e.g. \"**/backup/**\"."
        },
        "gessq.releaseNotes.showOnUpdate": {
          "type": "boolean",
          "default": true,
          "description": "Open the release notes once after installing or updating the extension. The command \"GESS Q.: Release Notes anzeigen\" stays available when this is off."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run check && node esbuild.js --production",
    "compile": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "typecheck": "tsc --noEmit -p ./",
    "lint": "eslint .",
    "gen:language": "node tools/gen-language.js && prettier --write src/data/language.ts",
    "check": "npm run typecheck && npm run lint",
    "pretest": "npm run typecheck",
    "test": "jest",
    "clean": "rimraf --glob out \"*.vsix\"",
    "package": "npm run check && node esbuild.js --production && vsce package",
    "tools:pdf2glossary": "node ./tools/pdf2glossary.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/jest": "^29.5.2",
    "@types/node": "^22.14.0",
    "@types/vscode": "1.85.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "eslint": "^9.24.0",
    "eslint-config-prettier": "^10.1.1",
    "jest": "^29.6.1",
    "ovsx": "^1.1.1",
    "pdf-parse": "^1.1.1",
    "prettier": "^3.5.3",
    "rimraf": "^6.1.3",
    "ts-jest": "^29.0.5",
    "typescript": "^5.6.0",
    "typescript-eslint": "^8.68.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "testMatch": [
      "**/__tests__/**/*.test.ts"
    ],
    "moduleNameMapper": {
      "^vscode$": "<rootDir>/src/__tests__/__mocks__/vscode.ts"
    }
  },
  "__metadata": {
    "id": "8369ddfc-3ff3-4da4-8712-89df76ef2c89",
    "publisherId": "7c9d6a5c-7f0a-4217-8a96-4f84eae98522",
    "publisherDisplayName": "Volker Dobler"
  }
}
