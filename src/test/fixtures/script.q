// Root of the integration-test project. `.vscode-test.mjs` opens this folder as
// the workspace, so the SymbolIndex anchors on this file and its includes.
#include "questions.q"
#include "labels.q"
#include "embedded.q"
